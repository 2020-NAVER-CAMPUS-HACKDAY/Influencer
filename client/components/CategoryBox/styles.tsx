import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  button: {
    background: 'yellow',
    width: '7rem',
    height: '7rem',
    color: 'black',
    border: 'none',
    cursor: 'pointer',
    outline: 'none',
  },
});

export default useStyles;
