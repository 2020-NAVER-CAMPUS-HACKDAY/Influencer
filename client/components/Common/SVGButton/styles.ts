import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  button: {
    background: 'none',
    border: 'none',
    outline: 'none',
  },
});

export default useStyles;
